SmalltalkCISpec {
  #loading : [
    SCIMetacelloLoadSpec {
      #baseline : 'GeoView',
      #directory : 'src',
      #platforms : [ #pharo ],
      #onConflict : #useIncoming,
      #onUpgrade : #useLoaded,
      #ignoreImage : true,
      #load: 'Core'
    }
  ],
  #testing : {
    #packages : [ 'GeoView-*' ]
  }
}
